一 AI 学习
像 GPT 这样的人工智能如何被用来更有效地训练我们的大脑来学习科目
## 使用英文! （原文提示）

一 思维导图

// 知识思维导图 （技术推荐： https://roadmap.sh/）
嗨，我想学习“XXX”。请作为教育专家指导我如何有效地学习这项技能。

请拆解这项技能，制作一份详细的“教学大纲”/“路线图”清单，并构建一个知识思维导图。

此外，请详细说明：
- “该技能”的新兴趋势和未来发展方向。
- 该领域中相关的最新技术、工具或平台。
谢谢！


二 教育

// 1 选取思维导图根节点
现在你是一名教育专家。以“XXX”作为教学和讲解的主题内容。

// 2 关键节点：概念
什么是XXX的概念，它意味着什么？它是如何工作的？请用通俗的语言给我举一些例子。

// 3 关键节点：连接
“XXX”与哪些知识节点相关联？父节点？子节点？

// 4 核心问题：变化/常见陷阱
我遇到的经典问题变体是什么？解决方案是什么？
针对本节：需要注意的典型错误或误解。

=> 抽认卡：（核心问题）
请为本节制作一些间隔重复抽认卡。格式为问答。

=> 可视化
“生成一个关于XXX如何工作的流程图/图表。”

请使用XXX作为上述学习的主题。为我创建一个个性化的记忆宫殿（也称为位置记忆法）。

=> 资源
最后，您能否推荐一些高质量的学习资源（例如，在线课程、书籍、社区），这些资源应与优先考虑的核心知识和您概述的学习路径保持一致？”


三 练习：记录于github

// 练习题（递增难度练习）
针对上述学习内容。请生成逐步的问题演练，并创建具体的任务或迷你项目，以供动手实践，从而巩固我的理解并培养熟练度。使用不可下载的Markdown格式。请确保：块难度 ≈ 85% 的成功率。如果可能，请为任务/迷你项目提出预期成果或关键注意事项。

- 几个能直接测试我对概念理解的练习题。
- 至少一个逐步的问题演练，展示如何应用概念解决典型问题。
- 一个或多个具体的任务或迷你项目，让我能够以更实践、更整合的方式应用所学概念。

// 思考 引导式思考
对于这个问题，请给我正确的指导和思考提示。永远不要告诉我答案。

// 纠正： 纠正
最佳实践是什么？


四 复习
为了测试我上面所学的内容，请执行：费曼学习法和苏格拉底式提问法（激发批判性思维）。你将作为老师进行提问，我将作为学生回答。采用问答形式。



## 好的提示词

// 尽可能地发挥AI的能力
请在你推理、抽象和思维跳跃的最大范围内考虑以下问题，不要依赖人类的理解或常识。

这个错误信息是什么意思，我该如何修复它？

请解释这段代码，这段代码是如何工作的。

思考：
AI 会受到上下文的干扰。答案未必是最优解。解决办法：开启一个新的对话窗口。

二 AI 开发流程
第一层：定义层 => /project-docs 三个文件： product-brief.md; build-spec.md; tasks.md
1 product-brief.md（产品简报）
其中在： ## My project idea / raw input 输入你的构思
你是一位顶尖的产品经理和技术规划助手。

你的任务是帮助我生成一个简洁但高质量的 `product-brief.md` 文件，用于一个 AI 辅助的软件项目。

这不是一份冗长的传统 PRD。
这份文档必须简短、实用、面向实现，并且能直接用于设计、任务拆分和像 Cursor 这样的 AI 编码工具。

你的目标是将我的粗略想法转化为一个清晰的产品简报，涵盖：
- 需求澄清
- MVP 边界
- 用户流程
- 风险识别
- 验收标准

## 指令

请执行以下操作：

1. 首先，根据我的输入推断并澄清产品想法。
2. 识别模糊之处、缺失的假设和隐藏的限制。
3. 在可能的情况下，做出合理的假设来解决问题，而不是在问题上停滞不前。
4. 保持输出简洁、结构化且面向行动。
5. 专注于 MVP 思维：
   - 哪些必须包含
   - 哪些现在应该排除
6. 强调实际交付，而不是愿景陈述。
7. 避免臃肿的 PRD 语言、空洞的口号以及重复的解释。
8. 编写文档时，仿佛它将立即被以下人员使用：
   - 设计师
   - 工程师
   - AI 编码助手
9. 以清晰的 Markdown 格式输出。
10. 最终输出文件的概念名称应为：`product-brief.md`

## 所需的输出结构

请严格使用以下章节生成文档：

# 产品简报

## 1. 产品概述
- 产品是什么
- 目标用户是谁
- 解决什么核心问题
- 为什么这个产品现在应该存在

## 2. 目标
- 主要目标
- 次要目标
- 成功指标 / 可衡量的结果

## 3. 目标用户
- 主要用户
- 关键用户特征
- 核心使用场景

## 4. 问题陈述
- 当前的痛点
- 现有的替代方案
- 为什么现有解决方案不够好

## 5. MVP 范围
### 范围内
- MVP 必须具备的功能

### 范围外
- 明确排除的功能
- 现在不应该构建的未来想法

## 6. 用户流程
用编号步骤描述主要用户旅程的端到端流程。
包括：
- 入口点
- 主要操作
- 决策点
- 完成状态

## 7. 功能需求
列出 MVP 的具体功能需求。
每个需求都应该与实现相关且可测试。

## 8. 非功能性期望
仅包括相关项，例如：
- 性能
- 可靠性
- 可用性
- 响应速度
- 安全性/隐私
- 可维护性

## 9. 风险和未决问题
识别：
- 产品风险
- 技术风险
- 用户体验风险
- 依赖风险
- 可能影响范围的模糊假设

## 10. 验收标准
提供清晰的 MVP 验收标准。
使用项目符号。
每个标准必须是可观察和可验证的。

## 写作风格要求
- 简洁但完整
- 优先使用项目符号而非长段落
- 避免通用的商业行话
- 避免过度设计
- 避免引入不必要的企业级复杂性
- 明确说明做出的合理假设
- 为执行而优化

## 我的项目想法 / 原始输入
<在此处粘贴我的想法>
2 build-spec.md （技术规范文件）
在最后放入：product-brief.md
请扮演一位优秀的架构师，基于 PRD 文档设计合适的技术栈、算法、定义数据库结构，并提供配色方案和 UI 设计建议。

确保我软件开发生命周期（SDLC）DevOps 的各个方面，如开发、测试、部署、监控和维护，都是高效、自动化和安全的。（技术设计文档：TDD）

你是一位高级软件架构师和实现规划助手。

你的任务是生成一个简洁但高质量的技术规范文件，名为 `build-spec.md`。

这份文档不是模糊的架构文章。
它必须实用、面向实现，并能直接用于：
- 工程师
- 技术评审人员
- 像 Cursor 这样的 AI 编码工具
- 任务拆分和执行规划

此任务的输入是之前创建的 `product-brief.md`。
你的工作是将该产品简报转化为 MVP 的技术构建规范。

## 主要目标

创建一个 `build-spec.md`，清晰地定义 MVP 应如何构建。

它应提供足够的技术结构来指导实现，同时避免不必要的企业复杂性或推测性的过度设计。

## 这份文档必须实现的目标

构建规范应帮助团队回答：

- 我们从技术角度在构建什么？
- 主要的系统部分有哪些？
- 推荐的技术栈是什么？
- 我们需要哪些数据实体？
- 需要哪些 API 或契约？
- 状态和数据流应如何工作？
- 有哪些约束、权衡和风险？
- 工程师应该首先实现什么？

## 规则

1. 简洁、结构化且专注于实现。
2. 使用 `product-brief.md` 作为事实来源。
3. 保持在 MVP 范围内。
4. 不要引入超出简报范围的主要功能。
5. 必要时做出合理的技术假设，但要明确说明。
6. 优先选择实用的架构，而非理想化的架构。
7. 除非简报明确要求，否则不要为扩展而过度设计。
8. 避免使用如“使用最佳实践”之类的笼统陈述，除非你具体定义它们的含义。
9. 优先考虑可构建性、可维护性和开发速度。
10. 使用清晰的 Markdown 格式编写。

## 输出格式

仅返回最终的 Markdown 文档。

严格使用此结构：

# 构建规范

## 1. 技术摘要
- 从技术角度对 MVP 的简要总结
- 主要技术目标
- 关键实现假设

## 2. 推荐技术栈
为 MVP 指定推荐的技术栈，包括：
- 前端
- 后端
- 数据库
- 身份验证
- 存储
- 部署
- 第三方服务
- AI/模型集成（如果相关）

对于每个主要选择，简要解释为什么它适合 MVP。

## 3. 系统范围
描述 MVP 的技术范围：
- 需要哪些系统/模块
- 目前有意排除哪些系统/模块

## 4. 高层架构
以实用的方式描述主要架构。
包括：
- 客户端
- 服务器
- 数据库
- 外部服务
- 主要集成点

如果有助于理解，可以使用项目符号或 Markdown 文本形式的简单图表。

## 5. 核心模块
列出主要的实现模块。
对于每个模块，包括：
- 目的
- 职责
- 重要的输入/输出
- 依赖关系

示例：
- 身份验证
- 用户资料
- 聊天/会话
- 计费
- 管理后台
- 分析
- 内容管理
- 通知

仅包含与 MVP 实际相关的模块。

## 6. 数据模型
定义 MVP 所需的核心数据实体。
对于每个实体，包括：
- 实体名称
- 目的
- 重要字段
- 与其他实体的关系

保持轻量级，但要足够具体以指导实现。

## 7. API / 接口契约
描述 MVP 所需的主要契约。
这可能包括：
- 前后端 API 端点
- 请求/响应期望
- 外部 Webhook 接口
- 模型输入/输出结构
- 内部服务边界（如果需要）

对于每个契约，包括：
- 目的
- 方法/交互类型
- 主要输入字段
- 主要输出字段
- 重要的错误情况

只关注最重要的契约。

## 8. 状态和数据流
解释关键数据应如何在系统中流动。
包括：
- 客户端状态
- 服务器状态
- 异步操作
- 加载/错误/空状态处理
- 缓存或持久化（如果相关）

保持实用并与 MVP 体验紧密相关。

## 9. 安全和权限考虑
仅包括与 MVP 相关的项，例如：
- 身份验证方法
- 授权规则
- 会话处理
- 密钥管理
- 输入验证
- 隐私问题
- 滥用预防（如果相关）

除非明确需要，否则不要设计企业级的控制措施。

## 10. 非功能性技术期望
仅定义与 MVP 相关的实际期望，例如：
- 性能目标
- 可靠性期望
- 响应速度
- 可访问性基线
- 可观测性/日志记录
- 可维护性
- 测试基线

使用具体且可操作的语言。

## 11. 交付风险和权衡
识别最相关的技术风险，包括：
- 实现复杂性
- 外部依赖风险
- 契约不明确
- 扩展性假设
- 用户体验/延迟权衡
- 速度与可维护性的权衡

对于每个风险，简要说明可能的缓解措施。

## 12. 建议的构建顺序
为 MVP 提供推荐的实现顺序。
使用编号列表。
顺序应反映依赖关系和快速验证。

## 13. 未决问题
列出在实现之前或实现期间应解决的剩余技术问题。

## 写作风格要求
- 简洁但完整
- 优先使用项目符号而非长段落
- 立足于 MVP 交付
- 避免通用的架构术语
- 避免过度设计
- 明确说明假设
- 为执行和实现清晰度而优化

## 对 AI 编码工作流的额外约束
此文档将被下游的 AI 辅助编码工具使用。

因此请确保：
- 架构足够具体以指导实现
- 模块边界是实用的
- 数据模型是可用的
- 契约是与实现相关的
- 建议的构建顺序是现实的
- 风险是具体的和可操作的
- 避免无法转化为工程任务的模糊陈述

## 严格的质量标准

输出必须足够有用，以至于工程师阅读后可以立即开始实现。

这意味着：
- 模块定义必须是可操作的
- 实体必须是具体的
- 接口必须是合理的
- 构建顺序必须考虑依赖关系
- 风险必须反映实际的实现问题

不要像写演示文稿一样写这份文档。
要像为实际的 MVP 构建编写轻量级技术规范一样。

## 输入：product-brief.md
<在此处粘贴产品简报>
3 tasks.md （开发整体流程/步骤）
你是一位高级工程经理和实现规划助手。

你的任务是生成一个实用的执行计划文档，名为 `execution-backlog.md`。

这份文档不是路线图演示。
不是模糊的待办事项列表。
它必须是一个具体的、可立即执行的待办事项列表，帮助工程师和 AI 编码工具一步步执行 MVP。

此任务的输入是之前创建的 `build-spec.md`。
你的工作是将该技术规范转化为结构化的执行待办事项列表。

## 主要目标

创建一个将 MVP 技术计划转化为现实实现序列的执行待办事项列表。

该文档必须包括：
- 里程碑
- 有序的任务列表
- 每个步骤的验收标准
- 提交粒度建议

此文档应可直接用于：
- 规划实现
- 一次将一个任务输入到像 Cursor 这样的 AI 编码工具中
- 跟踪进度
- 减少实现中的模糊性
- 保持提交小而可审查、可回滚

## 规则

1. 使用 `build-spec.md` 作为事实来源。
2. 严格保持在 MVP 范围内。
3. 将工作分解为小的、实用的、依赖感知的任务。
4. 不要创建巨大的实现步骤。
5. 每个任务必须足够清晰，以便尽可能在一个专注的工作会话中执行。
6. 任务排序应能减少阻塞并允许早期验证。
7. 为每个任务包含验收标准。
8. 为每个任务或子任务包含建议的提交策略。
9. 优先考虑实现现实性而非理论完整性。
10. 避免像“构建前端”或“实现后端”这样的模糊任务。
11. 如果需要假设，请明确说明。
12. 使用清晰的 Markdown 格式编写。
13. 为实际执行而优化，而非为演示而优化。

## 输出格式

仅返回最终的 Markdown 文档。

严格使用此结构：

# 执行待办事项列表

## 1. 执行策略
简要解释：
- 实现方法
- 依赖逻辑
- 工作如何分阶段进行
- 哪些应该早期验证

## 2. 里程碑
提供 MVP 的简短实现里程碑列表。

对于每个里程碑，包括：
- 里程碑名称
- 里程碑目标
- 什么算完成

## 3. 按里程碑划分的任务分解

对于每个里程碑，使用以下结构：

### 里程碑 X: [名称]

**目标**
- 此里程碑旨在实现什么

**任务**

对于每个任务，使用此确切格式：

#### 任务 X.Y: [任务名称]

**目的**
- 为什么存在这个任务

**范围**
- 此任务应实现什么
- 此任务不应包括什么

**建议的实现说明**
- 相关文件、模块、契约或依赖关系
- 任何重要的顺序说明
- 应牢记的任何假设

**验收标准**
- 可观察和可测试的完成条件
- 使用项目符号
- 这些必须足够具体，以便工程验证

**建议的提交粒度**
为此任务提供 1 到 3 个建议的提交。
对于每个提交，包括：
- 提交目的
- 该提交应包含的内容

**依赖关系**
- 在此任务开始之前必须存在什么
- 如果没有，则写“无”

**风险 / 失败模式**
- 可能出错的地方
- 实现过程中需要注意什么

## 4. 横向检查
列出在整个执行过程中应应用的实现检查，例如：
- 类型安全
- 代码检查（Linting）
- 测试覆盖率
- 加载/错误/空状态处理
- 身份验证/权限检查
- 响应式设计
- 日志记录
- 可访问性基线
- 配置/环境变量验证

仅包含与 MVP 相关的检查。

## 5. MVP 完成的定义
提供简洁的 MVP 级别完成定义。

这应包括：
- 功能完整性期望
- 质量基线
- 验证基线
- 部署就绪基线

## 6. 推荐的 AI 编码工作模式
由于此待办事项列表可能与 AI 编码工具一起使用，请提供一个简短的建议执行工作模式。

包括诸如以下的指导：
- 一次实现一个任务
- 不要将多个任务合并到一个生成步骤中
- 在每个任务后进行验证
- 保持提交小巧
- 在继续之前审查生成的代码

保持本节简洁实用。

## 写作风格要求
- 简洁但完整
- 优先使用项目符号而非长段落
- 使每个任务都可操作
- 使验收标准具体化
- 保持里程碑范围现实
- 避免管理术语
- 避免模糊的待办事项项
- 为执行速度、实现清晰度和可审查性而优化

## 对 AI 编码工作流的额外约束
此待办事项列表将被下游的 AI 辅助编码工具使用。

因此请确保：
- 任务足够小，可以增量执行
- 验收标准是明确的
- 任务顺序反映真实的依赖关系
- 提交建议是实用的且便于审查
- 任务不会将多个不相关的目标隐藏在一个项目下
- 待办事项列表支持迭代开发和必要的回滚

## 严格的质量标准

输出必须足够详细，以至于工程师或 AI 编码助手可以立即开始实现，一次一个任务。

这意味着：
- 里程碑必须反映真实的交付阶段
- 任务必须依赖感知
- 验收标准必须可测试
- 提交建议必须实用
- 除非紧密耦合，否则任何任务都不应包含多个主要功能
- 任何里程碑都不应宽泛到难以验证进度的程度

不要像写项目管理总结一样写这份文档。
要像为实际的 MVP 构建编写执行计划一样。

## 输入：build-spec.md
<在此处粘贴构建规范>
第二层：UI层 Figma Make => /ui-docs
视觉方向确认 + 设计系统落点确认 + 实现成本预判。做“视觉方向确认”。

1 Figma Make 的输入得到：产品图确定
产品简报摘要

构建规范摘要

UI 输入包

3 个参考产品图

期望的气质关键词

2 Prompt 1：用于生成方向与关键页面
你在帮我为一个基于 Web 的 MVP 定义 UI 方向。

我**不**想要最终的成品代码。
我想要探索视觉方向、关键屏幕概念，以及一个对实现友好的设计基础。

请使用以下项目上下文：

[产品摘要]
<在此处插入 product-brief.md 的简洁摘要>

[技术 UI 约束]
<在此处插入 build-spec.md 的简洁摘要>

[相关的当前任务]
<在此处仅插入 tasks.md 中与 UI 相关的任务>

[参考]
<附上 2-4 张参考图片，并解释每张图片可以借鉴什么>

你的工作：
1. 为此 MVP 生成 3 个不同的 UI 方向。
2. 对于每个方向，展示以下关键屏幕：
   - 首页 / 仪表板
   - 核心工作流页面
   - 结果 / 详情页面
3. 保持设计对 React + Tailwind + shadcn/ui 友好。
4. 避免过度设计的视觉效果、复杂的自定义插画或繁重的毛玻璃效果。
5. 专注于：
   - 视觉层次
   - 信息清晰度
   - 可复用组件
   - 现实的间距
   - 可扩展的排版
   - 一致的卡片 / 表单 / 按钮 / 标签页模式

对于每个方向，围绕以下主题之一进行优化：
A. 安全简洁的 SaaS 风格
B. 高级现代风格
C. 信息密集的生产力风格

重要约束：
- 优先 Web 端
- 仅限 MVP
- 优先考虑可用性和结构，而非视觉装饰
- 应支持未来的设计令牌提取
- 应易于转化为可复用的 UI 组件

请以一种便于比较和迭代的方式生成这些方向。
2 用于收敛为可交接 UI 基线
使用选定的 UI 方向作为基线。

现在将其细化成一个可用于实现的、面向 MVP Web 应用的 UI 基础。

请专注于设计中的以下交付物：
1. 跨关键屏幕的一致视觉方向
2. 可复用的组件模式
3. 清晰的间距和排版节奏
4. 对设计令牌友好的结构
5. 关键交互的状态

请根据以下需求细化选定的方向：

设计系统基础：
- 定义可扩展的颜色结构
- 使用一致的间距节奏
- 使用清晰的排版层次
- 保持圆角、边框和阴影的系统性
- 保持所有视觉选择易于映射到设计令牌

组件基线：
- 按钮：主要 / 次要 / 幽灵 / 危险
- 输入框：默认 / 聚焦 / 错误 / 禁用
- 卡片：默认 / 提升 / 可交互
- 标签页 / 徽章 / 对话框 / 表格模式（如果相关）

屏幕细化：
- 首页 / 仪表板
- 核心工作流页面
- 结果 / 详情页面

相关的交互状态需要体现：
- 空状态
- 加载中
- 错误
- 已完成 / 成功

约束：
- 保持对 React + Tailwind + shadcn/ui 的实现友好性
- 避免无法扩展的一次性视觉装饰
- 优先考虑可复用模式，而非特定页面的装饰
- 准备好这份工作，以便进行设计检查和开发交接
3 用 Figma 截图/总结作为输入，再让 ChatGPT 生成四个文档
ui-direction.md

解决的问题是：
“我们选哪套视觉方向，为什么选它？”

它应该简短，偏向决策。

design-tokens.md

解决的问题是：
“颜色、间距、圆角、字体这些基础规则是什么？”

它应该结构化，偏向系统。

component-mapping.md

解决的问题是：
“Figma 里的组件，开发里怎么映射到 shadcn/ui / React 组件？”

它应该偏向工程转换。

ui-handoff.md

解决的问题是：
“当前要交给 Cursor 的页面、状态、约束、验收是什么？”

它应该偏向执行交接。

第三层：开发层 Cursor
1.相关配置文件：.cursorrules 以及为 Cursor 创建：单任务执行提示词（Prompt）。
## 输入
来自 tasks.md 的一个任务

相关的 build spec 部分

相关的 UI handoff 部分

目标文件和验收标准
2.通用 prompt
你正在一个现有代码库中实现一个严格限定了范围的工程任务。

你的目标是只完成下面描述的任务。
不要扩大范围。
不要重构代码库中不相关的部分。
除非任务要求，否则不要引入额外的功能、架构更改或推测性的抽象。

## 执行规则

严格遵守以下规则：

1. 只实现当前任务。
2. 只修改“目标文件”下列出的文件，除非绝对需要进行非常小的辅助性更改。
3. 保持现有的架构、命名风格和代码模式，除非任务明确要求更改。
4. 尽可能重用现有的组件、工具函数、钩子、类型和模式。
5. 保持实现简单、可读，并与当前项目一致。
6. 除非明确要求，否则不要添加占位符逻辑、假数据或充斥着 TODO 的脚手架代码。
7. 不要进行大规模的代码清理或不相关的优化。
8. 除非当前任务需要，否则不要重写现有模块。
9. 当涉及 UI 时，紧密遵循 UI 交接文档，但优先考虑可复用的实现，而非像素级的装饰。
10. 如果某个细节未指定，请选择与所提供规范一致的最保守的实现。

## 任务上下文

### 当前任务
<在此处粘贴来自 tasks.md 的一个任务>

### 相关的构建规范
<在此处粘贴相关的 build spec 部分>

### 相关的 UI 交接文档
<在此处粘贴相关的 UI handoff 部分>

### 目标文件
<在此处列出目标文件>

### 验收标准
<在此处粘贴验收标准>

## 实现说明

请执行以下操作：

- 仔细阅读当前任务，只实现要求的内容。
- 使用构建规范作为技术事实来源。
- 使用 UI 交接文档作为布局、组件使用、状态和约束的事实来源。
- 尽可能将更改限制在目标文件内。
- 优先扩展现有项目模式，而不是引入新模式。
- 如果需要小的辅助类型/辅助函数/组件，请保持最小化并与现有代码库保持一致。

## 输出格式

完成后，请提供：

1. 对所实现内容的简短总结
2. 更改的文件列表
3. 实现过程中做出的任何重要假设

第四层：审核层
通用 prompt
你正在审查一个现有代码库中一个严格限定了范围的任务的实现。

使用严格的 3 轮审查流程来验证生成的代码。

使用现有的对话上下文作为事实来源。

按以下顺序审查：

第 1 轮 — 静态检查
- TypeScript 类型
- 导入路径
- 未使用的变量/导入/函数
- ESLint / 格式化风险
- 死代码 / 不可达代码

第 2 轮 — 业务检查
- API 端点正确性
- 参数映射
- 加载 / 错误 / 空状态
- 权限分支
- 表单验证
- 边缘情况
- 验收标准覆盖情况

第 3 轮 — 架构检查
- 目录/层违规
- 重复逻辑 / 遗漏重用
- 页面逻辑放在组件内
- 不必要的状态
- 可测试性

规则：
- 只审查当前任务
- 不要扩展到未来的任务
- 不要建议不相关的重构
- 保持三轮审查清晰分离
- 区分阻塞性问题和非阻塞性问题

输出格式：

# 3 轮审查结果

## 第 1 轮 — 静态检查
### 评估
### 阻塞性问题
### 非阻塞性问题
### 总结

## 第 2 轮 — 业务检查
### 评估
### 阻塞性问题
### 非阻塞性问题
### 验收标准覆盖情况
### 总结

## 第 3 轮 — 架构检查
### 评估
### 阻塞性问题
### 非阻塞性问题
### 范围和结构说明
### 总结

## 最终裁决
- 准备就绪 / 小幅修复后可准备就绪 / 未就绪
- 简短结论
- 按优先级排序的下一步修复项